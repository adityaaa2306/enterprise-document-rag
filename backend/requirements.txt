# === FastAPI Server ===
fastapi
uvicorn[standard]
python-dotenv
pydantic-settings
python-multipart  # For file uploads
requests          # For calling carbon APIs + NIM ranking

# === Authentication ===
passlib[bcrypt]   # kept for compatibility; hashing uses bcrypt directly
bcrypt
python-jose[cryptography]  # JWT tokens

# === Database ===
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1   # PostgreSQL driver (production)
chromadb                # File-based vector database (NOT migrated to Postgres)

# === Object storage (Phase 2 — R2 / S3; local backend needs no extra deps) ===
boto3>=1.34

# === Agentic Core ===
langgraph         # The "Brain" / Orchestrator

# === AI Agents (NVIDIA NIM — OpenAI-compatible) ===
openai>=1.55.3

# === Triage Agent (Visual Document Parsing) ===
unstructured[all-docs]
PyPDF2
