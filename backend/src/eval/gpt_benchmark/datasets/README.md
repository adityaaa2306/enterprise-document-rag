"""
Optional JSON datasets for quality evaluation.

Place ``smoke.json`` / ``full.json`` here to override built-in references.
Schema: list of {question, reference_answer, document_id?, tags?}
or { "items": [ ... ] }.
"""
