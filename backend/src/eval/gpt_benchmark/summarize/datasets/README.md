Optional JSON reference summaries for summarization quality scoring.

Schema: list of `{document_id?, filename?, reference_summary, tags?}`
or `{ "items": [ ... ] }`.

Place `summarization-smoke.json` here to override the built-in attendance reference.
